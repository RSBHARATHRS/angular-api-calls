import { CustopmPipePipe } from './custopm-pipe.pipe';

describe('CustopmPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustopmPipePipe();
    expect(pipe).toBeTruthy();
  });
});
